---
import { getCategories } from "../../lib/data";

export function getStaticPaths() {
  return getCategories().map((category) => ({
    params: { slug: category.slug },
  }));
}

const slug = Astro.params.slug ?? "";
return Astro.redirect(`/tools/category/${slug}/`, 301);
---
